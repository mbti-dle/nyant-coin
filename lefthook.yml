pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: npx lint-staged

pre-push:
  parallel: true
  commands:
    type-check:
      run: npm run type-check
    audit-check:
      run: npm audit